# راهنمای استفاده از Server24

## 📋 پیش‌نیازها

- سرور Ubuntu/Debian با دسترسی root
- دامنه یا ساب‌دامین که به IP سرور شما اشاره کند
- توکن ربات تلگرام (از @BotFather دریافت کنید)

## 🚀 نصب

### روش 1: نصب مستقیم از GitHub

```bash
bash <(curl -s https://raw.githubusercontent.com/saeed-rahimi/saeedrahimi/main/install.sh)
```

### روش 2: نصب دستی

1. کلون کردن ریپازیتوری:
```bash
git clone https://github.com/saeed-rahimi/saeedrahimi.git
cd saeedrahimi
```

2. اجرای اسکریپت نصب:
```bash
sudo bash install.sh
```

3. در حین نصب، اطلاعات زیر را وارد کنید:
   - توکن ربات تلگرام
   - ایدی عددی ادمین تلگرام
   - دامنه یا ساب‌دامین

## 🔧 تنظیمات پس از نصب

### بررسی وضعیت سرویس‌ها

```bash
systemctl status server24-api
systemctl status server24-bot
systemctl status xray
systemctl status nginx
```

### مشاهده لاگ‌ها

```bash
# لاگ API
journalctl -u server24-api -f

# لاگ ربات
journalctl -u server24-bot -f

# لاگ Xray
journalctl -u xray -f
```

### ری‌استارت سرویس‌ها

```bash
systemctl restart server24-api
systemctl restart server24-bot
systemctl restart xray
systemctl restart nginx
```

## 📱 استفاده از ربات تلگرام

1. ربات را در تلگرام پیدا کنید
2. دستور `/start` را ارسال کنید
3. از منوی ربات استفاده کنید:
   - خرید حجم
   - مشاهده وضعیت سرویس
   - مشاهده پروفایل
   - مدیریت کیف پول
   - ارسال تیکت پشتیبانی

## 🌐 دسترسی به پنل وب

پس از نصب، پنل وب در آدرس زیر در دسترس است:

```
https://your-domain.com
```

برای ورود، از ایدی تلگرام خود استفاده کنید.

## 👨‍💼 پنل مدیریت

برای دسترسی به پنل مدیریت:
1. با ایدی ادمین وارد شوید
2. به بخش "پنل مدیریت" بروید

قابلیت‌های پنل مدیریت:
- مشاهده لیست کاربران
- مدیریت کانفیگ‌ها
- ساخت کانفیگ دستی
- مشاهده آمار سیستم

## 🔐 امنیت

- تمام ارتباطات از طریق HTTPS انجام می‌شود
- فایروال به صورت خودکار تنظیم می‌شود
- دسترسی به API محدود است

## 🛠️ عیب‌یابی

### مشکل در نصب SSL

```bash
certbot certonly --nginx -d your-domain.com
```

### مشکل در اتصال ربات

1. بررسی توکن ربات
2. بررسی لاگ ربات: `journalctl -u server24-bot -f`

### مشکل در Xray

1. بررسی کانفیگ: `cat /usr/local/etc/xray/config.json`
2. بررسی لاگ: `journalctl -u xray -f`

## 📞 پشتیبانی

برای مشکلات و سوالات، از بخش تیکت در پنل استفاده کنید.

