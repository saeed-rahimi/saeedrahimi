# Server24 - پنل فروش کانفیگ Xray-core

پروژه کامل برای فروش و مدیریت کانفیگ‌های Xray-core با FastAPI، ربات تلگرام و پنل وب ساده.

## ✨ ویژگی‌ها

- ✅ نصب کاملاً خودکار با یک دستور
- ✅ پنل وب فارسی و راست‌چین
- ✅ ربات تلگرام با رابط کاربری فارسی
- ✅ مدیریت کامل کانفیگ‌های Xray-core
- ✅ سیستم کیف پول و خرید حجم
- ✅ سیستم تیکت پشتیبانی
- ✅ پنل مدیریت کامل
- ✅ SSL خودکار با Certbot
- ✅ امنیت بالا و فایروال

## 🚀 نصب سریع

```bash
bash <(curl -s https://raw.githubusercontent.com/YourGit/server24/main/install.sh)
```

در حین نصب، اطلاعات زیر را وارد کنید:
- توکن ربات تلگرام (از @BotFather)
- ایدی عددی ادمین تلگرام
- دامنه یا ساب‌دامین

## 📋 پیش‌نیازها

- سرور Ubuntu 20.04+ / Debian 11+
- دسترسی root
- دامنه یا ساب‌دامین که به IP سرور اشاره کند
- حداقل 1GB RAM و 10GB فضای دیسک

## 🛠️ تکنولوژی‌ها

- **Backend**: FastAPI (Python 3.10+)
- **Database**: SQLite
- **Bot**: python-telegram-bot
- **Frontend**: HTML/CSS ساده (بدون فریم‌ورک)
- **Proxy**: Xray-core
- **Web Server**: Nginx
- **SSL**: Certbot

## 📁 ساختار پروژه

```
server24/
├── backend/          # FastAPI Backend
│   ├── main.py      # API اصلی
│   └── requirements.txt
├── bot/              # ربات تلگرام
│   ├── bot.py       # ربات اصلی
│   └── requirements.txt
├── frontend/         # صفحات HTML/CSS
│   ├── *.html       # صفحات مختلف
│   └── style.css    # استایل‌ها
├── scripts/          # اسکریپت‌های کمکی
│   └── xray_manager.py
├── configs/          # فایل‌های کانفیگ
├── install.sh        # اسکریپت نصب
└── README.md
```

## 🔧 دستورات مفید

### بررسی وضعیت سرویس‌ها
```bash
systemctl status server24-api
systemctl status server24-bot
systemctl status xray
```

### مشاهده لاگ‌ها
```bash
journalctl -u server24-api -f
journalctl -u server24-bot -f
journalctl -u xray -f
```

### ری‌استارت سرویس‌ها
```bash
systemctl restart server24-api
systemctl restart server24-bot
systemctl restart xray
```

## 📱 استفاده

### ربات تلگرام
1. ربات را در تلگرام پیدا کنید
2. دستور `/start` را ارسال کنید
3. از منوی ربات استفاده کنید

### پنل وب
پس از نصب، پنل در آدرس زیر در دسترس است:
```
https://your-domain.com
```

## 📖 مستندات

برای اطلاعات بیشتر، به فایل [USAGE.md](USAGE.md) مراجعه کنید.

## 🤝 مشارکت

برای مشارکت در پروژه، به فایل [CONTRIBUTING.md](CONTRIBUTING.md) مراجعه کنید.

## 📝 مجوز

MIT License

## ⚠️ هشدار

این پروژه فقط برای اهداف آموزشی و قانونی است. استفاده از آن برای فعالیت‌های غیرقانونی ممنوع است.
